<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme for light mode. -->
    <!-- Inherit from Material3.Light.NoActionBar for a full-screen Compose experience with Material 3 light theme. -->
    <style name="Theme.MediaPowerhouse" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Material 3 color attributes -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <!-- Status bar color. Using colorPrimary for consistency with M3 common patterns. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Navigation bar color for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
